%p
  #{@resource.email}様
%p パスワードをリセットするには以下のリンクをクリックして下さい。
%p= link_to 'パスワードを再発行', edit_password_url(@resource, reset_password_token: @token)
%p パスワードの再設定をリクエストしていない場合は、このメールを無視してください。
%p パスワードはそのまま残ります。
%p 風呂魂
